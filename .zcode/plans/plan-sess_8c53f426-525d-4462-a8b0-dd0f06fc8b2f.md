# Lunio 审查遗留问题修复方案（R1 收尾 / R4 / R5 / R10 / R14 / R34 / 5.3.2 / 5.3.5 / 测试补齐）

依据：`docs/code-review-report.md` + 用户 2026-08-29 决策。已确认：R5 顺带删除死字段、snooze 边界测试要补、sheet 骨架统一成 PrototypeSheetFrame 那套。

无表结构改动（不需要 build_runner）、无 iOS 原生改动（不需要 build ios）。

## 批次 A · R1 收尾：删除车辆路径补通知取消

- `lib/features/shell/shared/shell_actions.dart:40-52` `deleteCar`：删车成功后补 `await LunioNotificationService.instance.cancelLunioNotifications()`（清 8000/8900 系旧调度），沿用 settings_data 恢复/清空的既有模式（bump 同步代数 → 操作 → 显式取消 → invalidate）。停车 9001/9002 与车辆无关，不动。
- 根因（探索确认）：`notification_sync_controller.dart:164` 在 `car == null` 时直接 return，删最后一辆车后同步引擎不会重排，旧通知残留。
- 测试：`test/widget_test.dart` 补"删除最后一辆车后 8000/8900 系通知被取消"用例（参照 :1010 清空用例的 `mockAndroidNotifications` + cancelledIds 断言手法）。

## 批次 B · R4：业务校验收紧为"同日只允许一条记录"

- `lib/data/repositories/lunio_repository.dart:1292-1325` `_ensureRecordIsUnique`：把"同项目拦截 / 不重叠放行"两分支合并为一条 `StateError('这辆车当天已有保养记录，请编辑原记录')`；删除 duplicateItems 查询；doc 注释改写。**新文案必须保持"这辆车当天"开头**（`formatters.dart:126-128` 的错误翻译按该前缀透传，否则落入兜底文案）。
- `lib/data/database/app_database.dart:140-141` 表注释同步（消除"两层口径不同"说明）。
- `removeMaintenanceRecordItem`（:811）删到 0 项连记录一起删的逻辑保留（语义是"空记录无意义"，与同日多条无关）。
- UI 不加预检（与现状一致，提交后拦截，文案直接显示在表单里）。
- 测试：`test/data/database_test.dart` 补"同日不同项目也抛 StateError"用例（现有 `same car and date is unique` :1184 走的是同项目分支，仍通过）。

## 批次 C · R10：cancel 收敛到实际使用的 16 个 id

- `lib/core/notifications/lunio_notification_service.dart:274-282` `cancelLunioNotifications`：`for (id = 8000; id < 9000; ...)` 改为只取消 8000-8007 与 8900-8907（服务内定义常量：基础 id 列表 [8000, 8900] + occurrenceCount 8；id 分配点全库仅 reminder_notifications.dart:165/:179 两处，已核实段内无其他写入方）。文件头 id 分配表注释同步。
- `test/widget_test.dart:1045-1054` 的 8000 段断言从 `isNotEmpty` 收紧为精确 16 个 id 集合。

## 批次 D · R14 吞异常加日志 + R34 时区/日历（按已确认 3 步）

1. R14 三处补 `debugPrint`：
   - `lunio_repository.dart:1084` getParkingCountdown JSON 损坏 catch；
   - `settings_data.dart:378` 权限查询失败回退 catch；
   - `lunio_notification_service.dart:394-395` 时区获取失败。
2. R34 步骤 1：`_configureLocalTimezone`（:390-397）失败回退从 `tz.UTC` 改为 `tz.getLocation('Asia/Shanghai')`。
3. R34 步骤 2：日期步进改日历加减——`_nextOccurrence`（:431-446）的日/两周/三周从 `Duration(days: N)` 相加改为 TZDateTime 按字段构造；`reminder_notifications.dart:363-368` `snoozeUntilDate` 的 +15 天改为 LocalDate 日历加减（不走 24 小时累加）。monthly 已在 R18 修过（有钳制），不动。
4. 测试：
   - 时区获取失败（mock channel 抛错）→ 断言回退 Asia/Shanghai（`test/core/lunio_notification_service_test.dart`）；
   - 日历步进用例（顺带覆盖既有 monthly 用例旁）。

## 批次 E · 5.3.2 sheet 骨架统一 + 5.3.5 FormActions（用户已确认方向）

- **骨架**：保留 `PrototypeSheetFrame`（shared_widgets.dart:144-218，功能超集）为唯一骨架。迁移 6 处默认表面 sheet：reminder_list.dart:183、date_picker.dart:86、parking_countdown.dart:424/624、settings_data.dart 通知设置与手动日期两处 sheet（改为 transparent + PrototypeSheetFrame，消除 5 处手写 Padding）。迁移完成后删除 `LunioSheetScaffold`（lunio_components.dart:167-198）与 `_LunioDefaultSheetSurface`（modal_feedback.dart:164-210）及 `showLunioModalSheet` 的默认表面分支 / `showDragHandle` 参数。
- **FormActions**：shared_widgets.dart 新增共用表单底部动作行组件（取消按钮文案 / 主按钮文案 / 保存中文案 / 两个回调 / saving / enabled），迁移 9 处：records_page ×2、vehicles ×1、maintenance_items ×2、parking_countdown ×2、settings_data ×2；顺带把 settings_data.dart:479-490 唯一一处裸 `FilledButton` 统一为 `LunioPrimaryButton`。
- 已核实：test/ 下 0 处引用这三个骨架类型名，58 条 widget 测试全部断言文案而非结构，回归风险集中在视觉（圆角 28→30、间距微调）。DESIGN.md 同步（圆角统一 30、动作行组件口径）。

## 批次 F · R5：删除 maintenanceDueEnabled 死字段（用户已确认）

| 文件 | 改动 |
|---|---|
| `lib/domain/entities/notification_settings.dart` | 删字段、构造参数、copyWith 参数（:22/:34/:43/:51-52）及 R5 注释 |
| `lib/app/providers.dart` | 批量读 key 列表删 `'maintenanceDueEnabled'`、删组装行（:105/:113） |
| `lib/features/shell/profile/settings_data.dart` | 删写入项与硬编码 true（:393/:507）及注释 |
| `lib/features/shell/reminders/reminder_notifications.dart` | `noticeDueForRow` 删短路判断并去 settings 参数（:433-436，唯一调用点 :425） |
| `lib/features/shell/reminders/notification_sync_controller.dart` | inApp 签名删 `${settings.maintenanceDueEnabled}:` 段（:191） |
| `test/widget_test.dart` | 删 :1823 断言 |
| `AGENTS.md` | 重要偏好 key 列表删 maintenanceDueEnabled（:65） |

风险已知且无行为差异：值恒为 true，升级后首帧 inApp 签名变化触发一次幂等重算；偏好表残留的旧 key 行无人再读，无害。

## 批次 G · 测试补齐（用户指定清单）

1. **R3 调度竞态**：widget_test 新用例——mock 通知通道让第一轮 reschedule 慢（handler 内 `Future.delayed`），期间触发第二次数据变化，断言 pending 重跑执行、最终通知反映最新数据（两轮 cancel/zonedSchedule 都发生）。参照 :1010 用例手法。
2. **exact alarm 被拒降级**：`canScheduleExactNotifications` false + `requestExactAlarmsPermission` false → 断言 `zonedSchedule` 收到 `inexactAllowWhileIdle`。
3. **snooze 到期日边界**（用户已确认补）：写入 snooze 到期日 D → 断言 D+15 当天 `isSnoozed` 为 true（当天仍静默）、D+16 为 false（恢复提醒），锁死现有 `>=` 语义（reminder_notifications.dart:371-385）。
4. **displayPercentForThresholds 钳制边界**：把 reminder_notifications.dart:517-531 的私有函数迁到 domain 规则层（纯函数）并补用例：低于阈值不进位（99.6 → 99）、恰好阈值、负数/0 归 0。

## 批次 H · 文档与注释收尾

- `docs/code-review-report.md`：R1 标全部修复、R4/R10/R14/R34 标已修复、R5 标"确认为产品设计 + 死字段已删"、R32 标全修复（5.3.2/5.3.5 本轮完成）、§7 关键缺口清单更新，注明 2026-08-29。
- `docs/operations-manual.md`：同步"同一天只允许一条保养记录"新规则、删除车辆补通知取消、sheet/FormActions 结构变化。
- `DESIGN.md`：圆角统一 30、共用动作行组件。
- `AGENTS.md`：偏好 key 列表（批次 F）。
- 代码内对应"⚠ R4/R10/R14/R34"标注随修复删除。

## 验证顺序（串行）

1. `flutter analyze`
2. 定向测试：`flutter test test/data/database_test.dart test/core/ test/domain/`
3. 全量 `flutter test`
4. 手动冒烟建议项（无法自动化）：各 sheet 视觉（统一骨架后）、删除最后一辆车后通知栏清理（真机/模拟器）、恢复/清空流程回归。

## 主要风险

1. 批次 E 改动面最大（15 个 sheet 迁移 + 删 2 个组件），测试只锁文案，视觉需手动核对。
2. 批次 F 触动通知签名，升级首帧多一次幂等重算（已论证无行为差异）。
3. R3 竞态测试依赖时序 mock（Future.delayed），可能需要调延迟参数才能稳定复现交错。
4. R4 是产品口径变化（同一天不能再保存不同项目的多条记录），文案与 operations-manual 三处锁口径。