# CONTEXT.md — Lunio 领域词汇表

本文件只放业务词汇的定义，不写实现细节。改流程时若产生新词或词义变化，随手更新这里。

## 保养业务（既有词汇）

- **当前应用车辆**：底部三页共享的那辆"正在看的车"。切走后所有按车展示的数据都跟着换。
- **保养项目**：一种需要周期性做的保养（如机油、刹车油），带里程/时间提醒间隔。
- **保养记录**：某天在某辆车上做的一批保养项目，含日期、里程、费用、备注。
- **按周期 / 按项目**：记录页的两种视图。按周期 = 一条记录一张卡；按项目 = 记录×项目展开成行。

## 车型目录与动力类型（2026-09-01 定稿的词汇）

- **车型目录**：添加车辆向导"选品牌 → 选车型"两级选择器的只读数据。条目名以懂车帝原始车系名为准（含品牌前缀和 DM/EV 这类动力词），收在售与停售，不收海外/进口标记条目和未上市条目。
- **动力类型**：燃油 / 混动（油电混动）/ 插混（插电混动）/ 增程 / 纯电，五选一。添加车辆时由用户选择，决定用哪套默认保养模板；增程与插混共用一套模板。添加后不可修改。
- **推荐动力类型**：目录按车系名和品牌推断的预选值（如名字含 DM/PHEV 推插混、蔚来推纯电）。只用于添加向导预选，用户可改，不是车辆数据。
- **默认保养模板**：按动力类型预置的五组保养项目组合（增程与插混共用内容，各自一组）。添加车辆时按所选动力类型整组复制成车辆自己的保养项目，"恢复默认项目"也按动力类型取。按品牌+车型逐条存放模板的旧模式已废弃。
- **车型专属模板**：个别车型的默认保养项目组合，白名单式例外（目前只有思域的 civicFuel，14 项，比燃油通用组多燃油宝等 4 项）。用户选的动力类型与推荐一致时优先用它，否则仍走动力类型通用模板；不进数据库，不进备份（见 docs/adr/0004）。

## 加油预测（2026-08-31 新增功能的词汇）

- **加油预测**：一个独立工具页，估算"把油箱加满要花多少钱"。不属于保养记录，不写保养数据。
- **油品编号**：参与计算的油的种类，单选：92#、95#、98#、0#。全局一份，所有车共用。
- **省份油价**：国内各省每升油价不同，按省取价。省份选择全局一份，默认湖北。
- **手填油价**：用户手动录入的每升价格，按"省份+油品"组合填一个价。手填价优先于数据源拉到的价，自动拉取和手动刷新都不覆盖它；清除手填后恢复用数据源价格。手填价是临时数据，不进备份文件。
- **油箱容积**：一辆车的油箱总容量，单位升，每辆车各一份，是车的属性；添加或编辑车辆时填写，非必填。
- **剩余油量**：油箱还剩百分之几，每辆车各一份，范围 0%–100%（2% 一档），默认 50%。加油页上没有单独的设置区，通过档位列表体现：列表第一行停在哪一档，剩余油量就是哪一档。
- **加满金额**：加满油需要的钱 =（100 − 剩余油量）÷ 100 × 油箱容积 × 每升油价。
- **档位**：金额展示行。从 100% 到 0% 每 2% 一档；整个列表可上下滚动，滚动停稳后第一行所在的档位就是当前剩余油量（自动记录）；窗口内可见 5 档。
- **油价缓存**：最近一次拉到的油价数据（全国各省各油品单价 + 调价预告）。本地临时数据，不进备份文件。
- **油价数据源**：外部油价查询接口。Lunio 只认自己的字段（省份、油品、单价、生效日期、更新时间），外部接口通过适配器接入，换接口不改业务代码。
- **调价预告**：数据源页面附带的"下次调价日期 + 预计每升涨/跌金额（常是一个区间）"。全国一份；页面没给或解析不到时，涉及它的展示显示占位符，不算错误。
- **预估下次油价**：按调价预告算出的调价后每升价 = 当前生效每升价（手填优先）+ 预计变动额（区间取中值）。
- **加油预测开关**：开发者模式里的功能开关，控制"加油"底部 tab 的显示。关闭后 tab 消失，已填的数据保留。
