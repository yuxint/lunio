# CONTEXT.md — Lunio 领域词汇表

本文件只放业务词汇的定义，不写实现细节。改流程时若产生新词或词义变化，随手更新这里。

## 保养业务（既有词汇）

- **当前应用车辆**：底部三页共享的那辆"正在看的车"。切走后所有按车展示的数据都跟着换。
- **保养项目**：一种需要周期性做的保养（如机油、刹车油），带里程/时间提醒间隔。
- **保养记录**：某天在某辆车上做的一批保养项目，含日期、里程、费用、备注。
- **按周期 / 按项目**：记录页的两种视图。按周期 = 一条记录一张卡；按项目 = 记录×项目展开成行。
- **距上次（时间 / 里程）**：某个保养项目从上一次做到参照点经过的时间 / 里程。参照点看场景：提醒页详情是"到今天 / 当前里程"；按项目记录详情是"到这一条记录自己"（即该项目相邻两条记录的间隔）。没有上一次可算（该项目首条记录）或算出来为负（补录乱序）时显示占位符，不参与提醒计算。

## 车型目录与动力类型（2026-09-01 定稿的词汇）

- **车型目录**：添加车辆向导"选品牌 → 选车型"两级选择器的只读数据。条目名以懂车帝原始车系名为准（含品牌前缀和 DM/EV 这类动力词），收在售与停售，不收海外/进口标记条目和未上市条目。
- **动力类型**：燃油 / 混动（油电混动）/ 插混（插电混动）/ 增程 / 纯电，五选一。添加车辆时由用户选择，决定用哪套默认保养模板；增程与插混共用一套模板。添加后不可修改。
- **推荐动力类型**：目录按车系名和品牌推断的预选值（如名字含 DM/PHEV 推插混、蔚来推纯电）。只用于添加向导预选，用户可改，不是车辆数据。
- **默认保养模板**：按动力类型预置的五组保养项目组合（增程与插混共用内容，各自一组）。添加车辆时按所选动力类型整组复制成车辆自己的保养项目，"恢复默认项目"也按动力类型取。按品牌+车型逐条存放模板的旧模式已废弃。
- **车型专属模板**：个别车型的默认保养项目组合，白名单式例外（目前只有思域的 civicFuel，14 项，比燃油通用组多燃油宝等 4 项）。用户选的动力类型与推荐一致时优先用它，否则仍走动力类型通用模板；不进数据库，不进备份（见 docs/adr/0004）。

## 加油预测（2026-08-31 新增功能的词汇）

- **加油预测**：一个独立工具页，估算"把油箱加满要花多少钱"。不属于保养记录，不写保养数据。
- **油品编号**：参与计算的油的种类，单选：92#、95#、98#、0#。全局一份，所有车共用。
- **省份油价**：国内各省每升油价不同，按省取价。省份选择全局一份，默认湖北。省份清单（31 个省级行政区，不含港澳台）是领域数据，不随数据源走；数据源适配器只负责把省名映射到自己的取数地址（见 docs/adr/0001 附注），默认湖北是产品默认、由偏好门面兜底。
- **手填油价**：用户手动录入的每升价格，按"省份+油品"组合填一个价。手填价优先于数据源拉到的价，自动拉取和手动刷新都不覆盖它；不想要手填价时点价格旁的"重置"恢复数据源价格。手填价是临时数据，不进备份文件。
- **油箱容积**：一辆车的油箱总容量，单位升，每辆车各一份，是车的属性；添加或编辑车辆时填写，非必填。
- **剩余油量**：油箱还剩百分之几，每辆车各一份，范围 0%–100%（2% 一档），默认 50%。加油页上没有单独的设置区，通过档位列表体现：列表第一行停在哪一档，剩余油量就是哪一档。
- **加满金额**：加满油需要的钱 =（100 − 剩余油量）÷ 100 × 油箱容积 × 每升油价。
- **档位**：金额展示行。从 100% 到 0% 每 2% 一档；整个列表可上下滚动，滚动停稳后第一行所在的档位就是当前剩余油量（自动记录）；窗口内可见 5 档。
- **油价缓存**：最近一次拉到的油价数据（当前省份各油品单价 + 调价预告，ADR 0011）。本地临时数据，不进备份文件。
- **油价数据源**：外部油价查询接口。Lunio 只认自己的字段（省份、油品、单价、生效日期、更新时间），外部接口通过适配器接入，换接口不改业务代码。
- **调价预告**：数据源页面附带的"下次调价日期 + 预计每升涨/跌金额（常是一个区间）"。随省份详情页附带、随缓存一起存；页面没给或解析不到时，涉及它的展示显示占位符，不算错误。**过期预告**（调价日早于当前应用日期；调价发生在当日 24 时，当天仍算有效）按无预告处理，不参与预估下次油价。
- **生效价**：加油页当前使用的每升价 = 手填价优先，其次油价缓存里当前省份、当前油品的单价；都没有时按无价处理（价格显示占位符）。
- **生效预告**：过滤过期后的调价预告（过期判定见"调价预告"词条）。油价缓存里仍存站点原话，过期过滤只在生效预告这一处做，预估块与"调价后价格"列统一从它取。
- **预估下次油价**：按调价预告算出的调价后每升价 = 生效价 + 预计变动额（区间取中值）。
- **加油预测开关**：开发者模式里的功能开关，控制"加油"底部 tab 的显示。关闭后 tab 消失，已填的数据保留。

## 加油记录与加油费用（2026-09-22 重定义的词汇，ADR 0015）

- **加油记录**：一次加油的流水，五项输入字段——日期（上限今天，不能未来）、油品（默认 92#）、单价、应付金额、实付金额（选填）。同一辆车同一天可以有多条（长途一天两箱），没有"同日查重"；删车随车删除。不联动车辆里程——保养记录是车辆里程的唯一写源（沿 ADR 0014）。
- **应付金额**：加油机口径的钱 = 单价 × 容积，必填。
- **实付金额**：优惠后实际支付的钱，选填——没填 = 无优惠，统计与展示取应付金额。实付低于应付的差额记为**优惠**（记录行显示"省 ¥x"）。
- **加油容积**：这次加了多少升 = 应付金额 ÷ 单价，由 App 算好落库**预留**（页面暂不展示）。与车辆档案的**油箱容积**（车的属性、加满预估用）不是同一个概念。
- **加油费用口径**：费用统计里一笔加油计的钱 = 实付金额，没填取应付。统计页有独立"加油费用"卡（2026-09-23 改版：删年度行，只保留全历史连续月度柱状图——横向滚动、柱顶标金额，无任何按年统计的维度），记录页头部汇总行分"今年保养"与"今年加油"两笔钱。
- ~~满箱段 / 本段油耗 / 平均油耗 / 每公里油费~~：随 2026-09-22 模型重定义删除（记录不再记里程与是否加满，油耗口径失去输入）；将来要做油耗时基于预留的加油容积重新设计。

## 通知与提醒（2026-09-03 定稿的词汇）

- **保养到期通知**：保养项目快到期或已过期时，系统在早上发的汇总通知（会连续多天重复提醒）。里程更新提醒与之同类，提醒的是该更新车辆里程了。两者都有应用内弹窗版本。
- **应用内提醒弹窗**：同一套到期判断在 App 内弹出的提醒，和系统通知相互独立、可分别开关。
- **稍后提醒**：提醒弹窗上的"15 天内不再提醒"按钮。点了之后这一项的系统通知和应用内弹窗一起静默 15 天（按日历天算，截止当天仍静默，次日恢复）。
- **知道了**：提醒弹窗上的"知道了"按钮。只静默当天的应用内弹窗，系统通知照发。
- **通知协调器（LunioNotificationCoordinator）**：拥有上面这些提醒规则的通知域模块，管三件事：系统通知开关怎么跟随系统真实权限状态、数据被删除/清空/恢复时哪些通知要取消、"稍后提醒/知道了"的静默读写。界面只报告发生了什么，不自己碰通知细节。

## 保养记录项目费用（2026-09-06 定稿的词汇）

- **项目费用**：一条保养记录里单个保养项目花掉的钱。非必填，允许只给部分项目填。
- **材料费 / 工时费**：项目费用的两个组成部分（配件材料的费用 / 人工的费用），全部由用户手填，不做反推。
- **费用自动计算**：材料费、工时费**任一非空**（含 0；未填的一侧按 0 求和，2026-09-12 修订——0 是明确的"没花钱"，与未填不等价，原规则要求两者都大于 0）时，项目费用自动算成两者之和；所有已填的项目费用自动累加成记录总费用。自动算出的值允许手动修改（比如优惠让总费用低于合计）；字段一旦被手动改过，就不再被自动覆盖，清空该字段视为放弃手改、恢复自动跟随。编辑历史记录打开时，存量费用与自动算结果不一致的（如优惠改价）直接视为已手改，不会被自动算链冲掉。
- **费用不一致提示**：项目费用与"材料费+工时费"不一致（材料、工时任一非空时比，未填一侧按 0），或总费用与项目费用合计不一致（有项目费用时），把不一致的那个数字红字显示、该行标黄色警告角标。纯提示，不拦截保存——优惠等造成的差异允许保存。
- **详细模式 / 简洁模式**：新增/编辑保养记录表单的两种填写模式。简洁模式是默认，只填记录总费用；详细模式通过表单内开关进入，可按项目填材料费/工时费/项目费用。开关状态不跨会话记忆；编辑已带项目费用的记录时自动进详细模式；切换模式不删除已填的项目费用。
- **记录详情弹窗**：记录页点击记录卡（按周期）或项目行（按项目）弹出的详情。按周期看整条记录（含所有项目费用），按项目只看该项目的费用。总费用只在按周期弹窗展示（副标题 + 指标格）；按项目弹窗没有总费用概念。

## 费用统计与优惠（2026-09-20 定稿的词汇；2026-09-21 改版：改名"费用"、条形占比、其他段守恒；2026-09-23 改版：走势改年度柱、加油卡去年度维度）

- **费用统计**：当前应用车辆的费用全貌（总费用、汇总指标、项目占比、保养费用走势、加油费用、优惠），独立子页展示。只统计当前应用车辆一辆，不提供"全部车辆"或多车切换——想看别的车先去切换应用车辆。
- **项目计费值**：一个保养项目参与费用统计的金额，项目占比、优惠分摊与项目档案共用这一个口径：就是该项目填写的项目费用，没填则该项目不参与统计。材料费/工时费不做读侧兜底——"材料/工时任一有值 ⇒ 项目费用必有值"的数据不变量由写点的 normalizeItemCost 保证（2026-09-21 拍板删除兜底分支）。
- **总优惠（当次）**：一条保养记录里"项目费用合计 − 记录总费用"的差值，正值即这次保养少花的钱。差值 ≤ 0 按无优惠计，不做"负优惠/超支"展示；一个项目费用都没有的记录（简洁模式只填总费用）不参与优惠统计。
- **优惠分摊**：把一条记录的总优惠按各项目费用的权重摊到每个项目，得出项目优惠与项目实付（实付 = 项目费用 − 分摊优惠）；取整后各项目分摊之和必须正好等于总优惠。纯读时统计口径，不修改任何存储值，与"费用不一致提示"的软提示模型一致。
- **其他段（守恒）**：项目占比里"无项目可归属的费用"聚合行——简洁模式记录的全部费用 + 单条记录"总费用超出项目费用合计"的差额。守恒关系：总费用 ≡ Σ项目实付 + 其他段，永远成立；有优惠导致的差值（项目费用合计 > 总费用）不算缺口，走优惠分摊。其他段不是项目，不可下钻。
- **汇总指标**：保养次数（总次数 + 今年次数，不看费用是否填写）、单次均价（总费用 ÷ 总次数）、月均（总费用 ÷ 首条记录月到当月的自然月数，含中间无记录月，全程摊薄；不随任何图表联动）、上次保养（最后一条记录距今的天数）。
- **保养费用走势**（2026-09-23 改版：年度曲线改年度柱状图）：每年一柱 = 该年保养费用合计，跨度首条记录年 → 今年（中间无记录年补 0 灰桩），横向滚动、柱顶标金额、今年柱主色高亮；不足 2 条保养记录整卡不展示。不再提供"近一年/近半年"窗口切换（2026-09-21 拍板删除）。
- **项目档案**：点项目占比行打开的单项目下钻视图——次数、累计计费值/实付/优惠、单次均价（累计实付 ÷ 次数）与逐次明细（日期倒序：日期、里程、实付、分摊优惠）。只统计有项目费用的次数，费用全空的记录不进档案；"其他"段不可下钻。

## 记录合法性软提示（2026-09-17 定稿的词汇）

- **里程单调（非降）**：同一辆车的保养记录，里程应随日期不减（允许持平）。补录/编辑造出"晚的记录里程反而更低"的矛盾历史时，App 在表单第一步「下一步」弹软提示，弹窗里给出冲突参照记录的日期与里程，用户可选"仍要继续"（真实世界允许乱序补录）或"返回修改"。
- **软提示**：与"拦截"相对——只提醒、不拦截保存，两个选择都不写库。同日冲突不走软提示（同日查重/唯一约束已在前面兜住）；记录数据没加载完时跳过检查，保存时既有校验兜底。

## 停车倒计时（2026-09-12 定稿的词汇）

- **停车预警通知**：免费停车时长快用完时的提前系统通知。开始计时时还剩余至少 30 分钟才启用：剩余 15 分钟、剩余 5 分钟各提醒一次；保存时剩余不足 30 分钟则两条都不发，只有到点通知。预警和到点通知同属停车通知，随“结束”或清空数据一起取消，应用内不重复提示（卡片本身实时显示剩余时间）。

## 停车实时活动（2026-09-15 定稿的词汇）

- **实时活动（Live Activity）**：iOS 系统托管的常驻实时卡片，出现在锁屏、通知中心顶部和灵动岛，App 不在跑也自动走秒。Lunio 只用它承载停车倒计时：进行中显示剩余时间数和进度条（从满条递减、到点走空），到点后转“已到期”样式等用户处理（回 App 后切成正计时），随“结束”或清空数据一起撤除；删车、恢复备份、关通知总开关都不动它。与停车通知并存互补——通知管“提醒”，实时活动管“常驻展示”。
- **灵动岛**：实时活动在 iPhone 灵动岛内的紧凑呈现（图标 + 倒计时数字），仅灵动岛硬件（iPhone 14 Pro 起）显示；同一套实时活动在无岛设备上只出现在锁屏和通知中心。它不是独立于实时活动的功能，没有”只做岛、不做锁屏”的拆法。

## 桌面小组件（2026-09-16 定稿的词汇）

- **小组件**：iOS 桌面上的保养提醒概览挂件（小、中两个尺寸），展示当前应用车辆下最紧急的几个保养项目与车况总览，让用户不打开 App 也能看到车况。只做 iOS（Android 本轮不做）；停车倒计时不上桌面——常驻展示已由实时活动覆盖，桌面刷新做不到走秒，放上去是更差的重复品。点击小组件经 `widgetURL` 深链（scheme `lunio`）落在提醒页（2026-09-16 接入，ADR 0013 六轮修订）。
- **小组件快照**：小组件渲染的唯一数据来源——App 内按”当前应用车辆 + 当时有效的今天”算好的一份只读数据，小组件自己不做任何计算。数据每在 App 内变化一次（记保养、改项目、改里程、切车等）就重写一次；因此桌面数字是”上次打开 App 时的车况”，App 不运行就停在最后写入的状态。
- **预生成窗口**：快照自带未来 14 天逐日的翻页数据，”还剩 N 天”这类随日历变化的数字每天自动翻页；翻完停在最后一页，等下次打开 App 重写快照续上。窗口内系统到点自动换页，不需要 App 运行。
