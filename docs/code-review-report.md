# Lunio 代码审查报告（未修条目台账）

> 原审查日期：2026-08-25 · 审查范围：`lib/` 全部手写 Dart 文件（不含生成文件 `app_database.g.dart`）、`test/`、`android/`、`ios/`、`docs/`
>
> **2026-09-01 瘦身**：R1-R38 绝大多数已修复或确认为设计，已修条目正文删除，完整历史见本文件在 git 中的旧版本。当前只保留尚未处理的条目；编号沿用原报告、不重排，代码注释里引用的 R 编号（如 R16/R36）仍可对应到台账。
>
> R20（v1→v2 破坏性迁移删光数据）已随 docs/adr/0005 成为产品设计本身（版本不符即删库重建），按已关闭处理。
>
> 配套文档：`docs/operations-manual.md`（UI 操作 ↔ 代码对照手册）。

---

## 未修条目

**R9 · 停车倒计时到期后无任何状态清理**
- 位置：`lib/data/repositories/lunio_repository.dart`（`getParkingCountdown` 无过期判断）；`lib/features/shell/reminders/reminder_page.dart`（倒计时存在时"停车倒计时"按钮禁用）
- 推理：到点后卡片转为正计时"已到点"（设计如此），但偏好里的倒计时永不自动清除；用户必须手动点"结束"才能开始新倒计时。App 被杀重启后依然显示过期卡片。
- 建议：可接受的设计（保留了"已停多久"信息），但建议提供"已到点状态一键开始新计时"；或到期超过 N 小时自动清除 + 取消通知。

**R17 · 已过期倒计时的通知调度被静默吞掉**
- 位置：`lib/core/notifications/lunio_notification_service.dart`（先 cancel 旧通知，再 `if (!scheduledDate.isAfter(now)) return;`）
- 推理：表单允许选过去的入场时间（时间轮只能改时分秒、初始为当天），若免费时长已过：数据库显示倒计时进行中，但既无到点闹钟也无常驻通知，无任何提示（与 R9 叠加成"看起来在计时其实什么都不会发生"）。
- 建议：保存时校验 endsAt 必须在未来，或提示"该时刻已过期"。

**R30 · 过度提取与欠提取的组件**
- 原报告 §5 复用评估中的 5.2（过度提取）与 5.3（该提取未提取）已于 2026-08 处理完毕（详见 git 历史旧版）；组件提取粒度维持现状，仅记录，不再跟踪。

**R36 · 里程与日期的宽松规则**
- 编辑车辆里程可任意改小（无回退校验，与"记录触发只增"并存——用户手动改小是允许的）；
- 记录日期允许未来 365 天（时间进度计算有归 0 防护，不崩溃）。
- 属产品规则宽松而非 bug，列出待确认。

---

*操作步骤与代码的逐行对应关系见 `docs/operations-manual.md`。*
