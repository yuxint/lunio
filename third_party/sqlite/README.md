# SQLite Amalgamation

This directory contains the SQLite 3.53.0 amalgamation source used by
`package:sqlite3` native assets.

Source archive:

`https://sqlite.org/2026/sqlite-autoconf-3530000.tar.gz`

The source is kept in the repository so Android and CI builds do not depend on
downloading precompiled SQLite native assets from GitHub releases.
